/target
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/codetalker-wasm/target

# wasm-pack output. Generated by CI into the Pages artifact; building locally
# puts it here too. Not committed — it is a build product of this repository,
# not a source of it.
/web/pkg

# ACVP vectors, fetched rather than vendored. See tests/kat.rs for why the test
# fails loudly instead of skipping when this is absent.
/codetalker-core/tests/vectors

.DS_Store
